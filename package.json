{
  "name": "github-portfolio-mcp-server",
  "version": "1.0.0",
  "description": "An MCP server that exposes tools to query GitHub portfolio data — repos, languages, READMEs, and profile info. Runs as stdio (local) or HTTP/SSE (hosted).",
  "main": "dist/index.js",
  "bin": {
    "github-portfolio-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "start:http": "PORT=3000 node dist/index.js",
    "dev": "tsc --watch",
    "dev:http": "PORT=3000 tsc && node dist/index.js"
  },
  "keywords": ["mcp", "github", "portfolio", "ai"],
  "author": "Ben Brown",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "express": "^4.21.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^25.3.0",
    "typescript": "^5.9.3"
  }
}
